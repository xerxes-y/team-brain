"""team-brain — role-aware org knowledge assistant built on memento's store.

Note: we deliberately do NOT re-export ``assist`` at the package level — that
name would shadow the ``teambrain.assist`` submodule. Import it explicitly:
``from teambrain.assist import assist``.
"""

__version__ = "0.1.0"
